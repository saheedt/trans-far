<div id="notice"><%= notice %></div>

<h1>Uploads</h1>

<div class="uploads-list">

  <ul>
    <% @uploads.each_with_index do |upload, index| %>
      <% if index % 2 == 0%>
        <li class="uploads-list-item striped">
          <div class="uploads-list-item-attr"><%= upload.name %></div>
          <div class="uploads-list-item-attr"><%= link_to 'Show', upload %></div>
          <div class="uploads-list-item-attr"><%= link_to 'Edit', edit_upload_path(upload) %></div>
          <div class="uploads-list-item-attr"><%= link_to 'Destroy', upload, method: :delete, data: { confrim: 'Are you sure?'} %></div>
        </li>
      <% else %>
        <li class="uploads-list-item">
          <div class="uploads-list-item-attr"><%= upload.name %></div>
          <div class="uploads-list-item-attr"><%= link_to 'Show', upload %></div>
          <div class="uploads-list-item-attr"><%= link_to 'Edit', edit_upload_path(upload) %></div>
          <div class="uploads-list-item-attr"><%= link_to 'Destroy', upload, method: :delete, data: { confrim: 'Are you sure?'} %></div>
        </li>
      <% end %>

    <% end %>
  </ul>

</div>
